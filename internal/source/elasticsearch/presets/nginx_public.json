{
  "size": 0,
  "query": {
    "bool": {
      "filter": [
        {"range": {"@timestamp": {"gte": "{{.From}}", "lte": "{{.To}}"}}},
        {"term": {"source_text": "pi-nginx"}},
        {"range": {"http.response.status_code": {"gte": 400}}}
      ]
    }
  },
  "aggs": {"breakdown": {"terms": {"field": "client.ip", "size": 10}}}
}
